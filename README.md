# ЙоТик M2  — умная теплица на ESP32

Умная теплица на ESP32 с автоматическим климат-контролем, подсветкой, поливом и несколькими интерфейсами управления:

- локальный **веб-интерфейс** с современным UI (SPA на чистом HTML+CSS+JS);
- **Telegram-бот** с удобной клавиатурой и быстрым выбором профиля культуры;
- **физический дисплей TM1637** для отображения основных параметров без телефона/ПК.

Проект рассчитан на круглосуточную работу и максимально аккуратную автоматику (учёт времени суток, защита от «мигания» света и чрезмерного полива, лимит работы насоса и т.п.).

## Основные возможности

- 📡 **ESP32 + Wi-Fi**
  - Подключение к домашней сети (режим STA).
  - Режим точки доступа `YotikM2-Setup / YotikM2pass`, если подключение не удалось.
  - Настройка Wi-Fi напрямую из веб-интерфейса.

- 🌡 **Датчики**
  - BME280 — температура, влажность и давление воздуха.
  - BH1750 — освещённость (lux).
  - Аналоговый датчик влажности почвы.
  - Поддержка датчика температуры почвы (при наличии соответствующего сенсора).

- 🔁 **Умная автоматика**
  - Контроль климата по комфортным диапазонам (температура + влажность).
  - Три режима климата: Eco / Normal / Aggressive.
  - Управление:
    - вентилятором;
    - сервоприводом двери/форточки;
    - насосом полива;
    - подсветкой (реле света).
  - Учёт времени суток (часовой пояс Москва, NTP).
  - Защита от «мигания» света:
    - включение при низкой освещённости,
    - игнорирование lux, если свет уже включён, до наступления «ночи».
  - Полив только в заданном временном окне, с анализом тренда высыхания почвы.

- 🧠 **Профили культур**
  - Несколько встроенных профилей:
    - 1 — 🍅 помидоры  
    - 2 — 🥒 огурцы  
    - 3 — 🌿 зелень  
    - 4 — 🌺 гибискус  
  - Профиль задаёт:
    - комфортные диапазоны температуры и влажности;
    - целевую влажность почвы и гистерезис;
    - минимальную освещённость;
    - режим климата;
    - окно полива и час «отключения подсветки».

- 💬 **Telegram-бот**
  - Стартовое меню и /help.
  - Клавиатура с кнопками:
    - `📊 Статус` — краткий отчёт о состоянии;
    - `💧 Полив` — импульсный полив;
    - `⚙ Авто ВКЛ` / `⏸ Авто ВЫКЛ` — включение/выключение автоматики;
    - `🌡 Профили` — выбор культуры через отдельное меню (`🍅`, `🥒`, `🌿`, `🌺`);
    - `🔔 Увед. ВКЛ/ВЫКЛ` — уведомления о проблемах с климатом/датчиками.
  - Быстрая смена профиля одной кнопкой без текстовых команд.
  - Уведомления при выходе параметров за допустимые границы (если включены).

- 🖥 **Веб-интерфейс**
  - Современный тёмный UI: карточки, метрики, плавные подсветки.
  - Живое обновление данных по AJAX.
  - Разделы:
    - «Текущее состояние» — температура, влажность, давление, почва, lux.
    - «Настройки автоматики» — климат, почва, окно полива, подсветка, режим климата.
    - «Ручное управление» — насос, свет, вентилятор, дверь.
    - «Диагностика и сеть» — логи, Wi-Fi scan, ввод SSID/пароля.
  - Простая BASIC-авторизация:
    - логин: **admin**
    - пароль: **greenhouse** (можно поменять в `WebInterface.h`).

- 🔢 **Физический дисплей TM1637**
  - Плавный цикл отображения:
    1. Температура воздуха.
    2. Влажность воздуха.
    3. Влажность почвы.
    4. Освещённость в lux.
  - Период обновления задаётся в `Constants::DISPLAY_UPDATE_MS`.
  - Работает как «быстрый взгляд» без телефона.

- 💾 **EEPROM**
  - Все настройки (`SystemSettings`) сохраняются в EEPROM:
    - Wi-Fi SSID / пароль;
    - профили/пороговые значения;
    - окно полива, режим климата, час отключения подсветки и т.д.
  - Версия структуры контролируется (`SETTINGS_VERSION`), при изменении — заводские значения.

## Структура проекта

Основные файлы:

- `SmartGreenhouse.ino`  
  Точка входа. Инициализация:
  - загрузка настроек из EEPROM;
  - применение профиля культуры;
  - запуск модулей: `Devices`, `DisplayManager`, `Automation`, `WebInterface`, `TelegramBotHandler`;
  - настройка NTP (Московский часовой пояс);
  - основной `loop()`:
    - чтение датчиков;
    - вызов автоматики;
    - обновление дисплея;
    - обработка HTTP-запросов и Telegram-сообщений.

- `Config.h`  
  Общая конфигурация:
  - пины реле, TM1637, сервопривода, датчиков;
  - константы (лимит работы насоса, интервалы опроса, обновления дисплея и т.д.);
  - структуры:
    - `SystemSettings` — все пользовательские настройки;
    - `SensorData` — текущее состояние датчиков и устройства;
    - `DeviceConfig` — информация о наличии и состоянии железа;
  - настройки Telegram (`TelegramConfig`), Wi-Fi и т.п.

- `Devices.h / Devices.cpp`  
  Работа с железом:
  - инициализация реле, сервопривода двери, светодиодов (FastLED);
  - инициализация датчиков BME280, BH1750, датчика почвы;
  - функции чтения датчиков (`readSensors`, `readBME280`, `readBH1750`, `readSoil`);
  - управление:
    - насосом (`setPump` с импульсным режимом и дневным лимитом);
    - вентилятором (`setFan`);
    - освещением (`setLight`);
    - дверью (`setDoorAngle` + плавное движение);
  - учёт общей наработки насоса за текущие сутки.

- `Automation.h / Automation.cpp`  
  Вся логика автоматики:
  - `loop()` — вызывается из `SmartGreenhouse.ino`, но сама автоматика запускается с заданным интервалом (`AUTOMATION_INTERVAL_MS`).
  - Методы:
    - `handleClimate()` — контроль температуры/влажности;
    - `handleLighting()` — контроль подсветки;
    - `handleWatering()` — контроль полива;
    - `isNightTime()`, `isWithinWateringWindow()` — работа с временем;
    - `computeSoilDryingSlope()`, `recordSoilHistory()` — анализ тренда высыхания почвы.

- `Profiles.h / Profiles.cpp`  
  Профили культур:
  - `applyCropProfile(uint8_t id, SystemSettings &s)`:
    - подбирает комфортные диапазоны для температуры/влажности;
    - задаёт целевую влажность почвы и гистерезис;
    - режим климата (0/1/2);
    - окно полива;
    - час `lightCutoffHour` для подсветки.
  - Профиль 0 — кастомный (ручная настройка).

- `DisplayManager.h / DisplayManager.cpp`  
  Управление TM1637:
  - `begin()` — инициализация, максимальная яркость, очистка;
  - `update()` — по таймеру переключает режимы:
    - температура воздуха, влажность воздуха, влажность почвы, lux;
  - отдельные функции `showAirTemp`, `showAirHumidity`, `showSoilMoisture`, `showLux`.

- `EEPROMManager.h / EEPROMManager.cpp`  
  Управление EEPROM:
  - `begin()` — `EEPROM.begin(EEPROM_SIZE)`;
  - `loadSettings()` — загрузка, проверка версии, откат к дефолтам при несовпадении;
  - `saveSettings()` — запись и `EEPROM.commit()`;
  - `resetDefaults()` — сброс настроек к заводским значениям.

- `WebInterface.h / WebInterface.cpp`  
  HTTP-сервер и веб-UI:
  - Поднятие Wi-Fi (STA → при неудаче AP).
  - Маршруты:
    - `/` — HTML-страница с UI;
    - `/api/sensors` — JSON с показаниями;
    - `/api/settings` (GET/POST) — чтение/запись настроек автоматики;
    - `/api/control` (POST) — ручное управление насосом, светом, вентилятором, дверью;
    - `/api/diagnostics` — отладочная информация;
    - `/api/wifi_scan` — поиск сетей;
    - `/api/wifi_set` — установка SSID/пароля.
  - BASIC-авторизация (`ensureAuth()`).

- `TelegramBotHandler.h / TelegramBotHandler.cpp`  
  Обёртка над UniversalTelegramBot:
  - инициализация бота, проверка токена;
  - опрос `/getUpdates` с интервалом;
  - обработка команд и кнопок:
    - `/start`, `/help`;
    - `📊 Статус`, `💧 Полив`;
    - `⚙ Авто ВКЛ`, `⏸ Авто ВЫКЛ`;
    - `🌡 Профили` → отдельное меню с кнопками профилей;
    - `🔔 Увед. ВКЛ/ВЫКЛ`;
  - отправка уведомлений о состоянии датчиков и авариях.

## Логика автоматики

### 1. Время и NTP

- В `setup()` настраивается часовой пояс Москва (MSK) и NTP-серверы.
- Функция `printGreenhouseTime()` периодически выводит текущие дату/время в Serial.
- Функции автоматики (`isNightTime`, `isWithinWateringWindow`) используют `getLocalTime()`:
  - при отсутствии времени (**нет NTP**) подсветка блокируется как «ночь» (чтобы не работать не по расписанию).

### 2. Климат (температура и влажность)

В `handleClimate()`:

- Берутся:
  - текущая температура и влажность (`SensorData`);
  - настройки комфорта (`SystemSettings.comfortTempMin/Max`, `comfortHumMin/Max`);
  - аварийные пороги (`safetyTempMin/Max`);
  - режим климата (`climateMode`: 0/1/2).

- Сначала проверяются **аварийные пороги**:
  - если слишком холодно — дверь закрывается, вентилятор выключается;
  - если слишком жарко — дверь открывается, вентилятор включается, независимо от «комфортных» границ.

- Далее рассчитываются «горячие» пороги в зависимости от `climateMode`:
  - Eco — более мягкие пороги включения вентиляции;
  - Aggressive — более раннее и сильное реагирование на рост температуры/влажности.

- Состояния:
  - **Нет вентиляции** — дверь закрыта, вентилятор выключен.
  - **Слабая вентиляция** — вентилятор или дверь, в зависимости от условий.
  - **Сильная вентиляция** — и дверь, и вентилятор.
- Есть минимальные интервалы между изменениями (антидребезг по времени) для двери и вентилятора.

### 3. Освещение (анти-мигание + учёт времени)

Функция `handleLighting()`:

- Берёт текущий lux с BH1750.
- Вычисляет `isNightTime()`:
  - ночь — если `час >= lightCutoffHour` **или** `час < 6`;
  - при ошибке времени — считаем, что ночь (подсветка заблокирована).

Алгоритм:

1. **Ночь**  
   - Подсветка *всегда* выключена:  
     если была включена — принудительно отключается.
2. **День, нет данных lux**  
   - Состояние света не меняется (не рискуем включать/выключать вслепую).
3. **День, есть lux**  
   - Порог включения: `LUX_ON_THRESHOLD = 60`.
   - Если свет **уже включен**:
     - lux **игнорируется** — свет остаётся включенным до наступления ночи.
     - → это как раз решает проблему «мигания»: свет не выключится от того, что сам себя подсветил.
   - Если свет **выключен**:
     - при `lux < 60` — включаем свет;
     - при `lux >= 60` — не включаем.

В итоге:  
- свет включается при падении освещённости ниже 60 lux в дневное время;  
- после включения горит до `lightCutoffHour`, независимо от lux;  
- после наступления ночи (или при отсутствии времени) — выключен.

### 4. Полив

Функция `handleWatering()`:

1. Если показаний по влажности почвы нет — выходим.
2. Проверяется, находимся ли **в окне полива**:
   - `wateringStartHour` / `wateringEndHour`;
   - окно может:
     - быть в пределах одних суток (`start < end`);
     - переходить через ночь (`start > end`);
     - при `start == end` — окно считается «весь день».
3. Берутся:
   - `soilMoistureSetpoint` — целевая влажность почвы;
   - `soilMoistureHysteresis` — гистерезис;
   - нижний порог `lowTh = setpoint - hyst`;
   - верхний порог `highTh = setpoint + hyst`.

4. Рассчитывается тренд высыхания почвы: `computeSoilDryingSlope()`:
   - хранятся последние значения влажности + время в массивах (`recordSoilHistory`);
   - по ним считается наклон прямой (%/час);
   - если наклон адекватный — используется, иначе игнорируется.

5. Логика включения насоса:
   - **Очень сухо**: `moist < lowTh - 5`  
     → насос включается на ~1400 мс.
   - **Просто сухо**: `moist < lowTh`:
     - если `slope >= 0.1 %/час` (быстро сохнет) → насос ~1200 мс;
     - иначе (медленно сохнет) → насос ~800 мс.
   - **Достаточно влаги**: `moist > highTh` и насос сейчас включён →
     - насос выключается.

6. В `Devices::setPump()` есть дневной лимит по времени работы насоса, чтобы защититься от «утёкшей» логики или сломанного датчика.


## Веб-интерфейс

### Доступ

1. Устройство пытается подключиться к Wi-Fi в режиме STA:
   - SSID/пароль берутся из `SystemSettings` (EEPROM).
2. Если подключиться не удалось:
   - переходит в режим точки доступа:
     - SSID: `YotikM2-Setup`
     - пароль: `YotikM2pass`
   - IP точки: обычно `192.168.4.1`.
3. В браузере:
   - открываем `http://<IP_устройства>/`.
   - вводим:
     - логин: `admin`
     - пароль: `greenhouse`.

### Страница

Состоит из нескольких карточек:

- **Текущее состояние**
  - Температура воздуха, влажность, давление.
  - Влажность почвы.
  - Освещённость lux.
  - Цветовые индикации: ok / warn / alarm.

- **Настройки автоматики**
  - Поля:
    - `comfortTempMin`, `comfortTempMax`;
    - `comfortHumMin`, `comfortHumMax`;
    - `soilMoistureSetpoint`, `soilMoistureHysteresis`;
    - `lightLuxMin` (для профилей);
    - `wateringStartHour`, `wateringEndHour`;
    - `lightCutoffHour`;
    - `climateMode` (0/1/2).
  - Изменения отправляются через `/api/settings` (JSON).

- **Ручное управление**
  - Насос:
    - «Импульс» (короткий полив),
    - ВКЛ / ВЫКЛ.
  - Свет:
    - ВКЛ / ВЫКЛ.
  - Вентиляция и дверь:
    - кнопки управления в зависимости от реализованных методов.

- **Диагностика и сеть**
  - Лог в `<pre id="diag">` с диагностикой.
  - Кнопка «Сканировать» → `/api/wifi_scan`;
  - Список найденных сетей;
  - Поля для ввода SSID/пароля и кнопка «Подключить к Wi-Fi»:
    - отправка на `/api/wifi_set`;
    - настройки сохраняются в EEPROM и производится переподключение.

---

## Telegram-бот

Чтобы использовать Telegram:

1. В `Config.h` заполнить:
   - `TelegramConfig::BOT_TOKEN` — токен бота;
   - `TelegramConfig::CHAT_ID` — при необходимости привязать к конкретному чату.
2. Пересобрать и прошить.

Основные команды и кнопки:

- `/start`
  - Приветствие и показ главного меню с кнопками (reply-keyboard).
- `/help`
  - Список команд и краткое описание возможностей.
- `📊 Статус`
  - Отчёт по:
    - температуре/влажности/давлению;
    - влажности почвы;
    - lux;
    - состоянию насоса/света/вентиляции/двери;
    - текущему профилю и окну полива.
- `💧 Полив`
  - Импульсный полив (через `g_devices.setPump(true, ...)`).
- `⚙ Авто ВКЛ` / `⏸ Авто ВЫКЛ`
  - Включение/пауза автоматики (`g_settings.automationEnabled`).
- `🌡 Профили`
  - Показ отдельного меню с кнопками:
    - `🍅 Помидоры`
    - `🥒 Огурцы`
    - `🌿 Зелень`
    - `🌺 Гибискус`
    - `⬅ Назад`
  - При выборе профиля:
    - вызывается `applyCropProfile(...)`,
    - настройки сохраняются в EEPROM,
    - бот сообщает о смене профиля.
- `🔔 Увед. ВКЛ/ВЫКЛ`
  - Включение/выключение регулярных проверок и уведомлений (`notificationsEnabled`).


## TM1637-дисплей

Используется библиотека `TM1637Display`.

- Пины берутся из `Pins::TM1637_CLK` и `Pins::TM1637_DIO`.
- В `DisplayManager::update()`:
  - каждые `DISPLAY_UPDATE_MS` переключается режим отображения:
    - воздух (°C),
    - влажность воздуха (%),
    - влажность почвы (%),
    - освещённость (lux).
- Формат вывода:
  - числа округляются и вписываются в 4 разряда;
  - при отсутствии данных — экран очищается.

## Сборка и зависимости

Минимальный набор библиотек (Arduino IDE / PlatformIO):

- **Датчики**
  - `BH1750`
  - `Adafruit BME280 Library`
  - `Adafruit Unified Sensor`
  - `Adafruit BusIO`
- **Управление**
  - `ESP32Servo`
  - `FastLED`
  - `TM1637`
- **Связь и сеть**
  - `WiFi` / `Network` (из ESP32 core)
  - `WebServer` (из ESP32 core)
  - `FS`
- **Telegram и JSON**
  - `UniversalTelegramBot`
  - `ArduinoJson`
- **Хранение**
  - `EEPROM`

Рекомендуемая плата: любая на базе **ESP32** (например, ESP32 DevKitC).  
В настройках IDE выбрать соответствующую плату и порт, затем прошить проект.

## Настройка под свою теплицу

1. Отредактировать пины в `Config.h` под своё железо.
2. При необходимости добавить новые профили культур в `Profiles.cpp`.
3. При первом запуске:
   - Подключиться к `YotikM2-Setup` → `192.168.4.1`;
   - зайти в веб-интерфейс `admin/greenhouse`;
   - отсканировать Wi-Fi, выбрать сеть, ввести пароль;
   - сохранить и дождаться переподключения.
4. Отрегулировать пороговые значения:
   - комфортные диапазоны температуры/влажности;
   - целевую влажность почвы;
   - окно полива и час `lightCutoffHour`.
